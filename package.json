{
  "name": "aiml-seat-allotment",
  "version": "1.0.0",
  "private": true,
  "description": "AIML Internal Examination Seat Allotment System (root wrapper for Render deploys)",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
